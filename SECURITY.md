# Security

Report vulnerabilities in the dashboard or adapter fetch path privately to
Age Old LLC. Do not open a public issue with an exploit.

The adapter proxy only accepts http(s) URLs, rejects loopback and private
networks, caps payload size, and does not execute code from datasets.

PEI scores are decision-support numbers, not secrets. Do not send
credentials, personal data, or classified series into the adapter field.
